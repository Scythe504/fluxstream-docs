---
title: Installation
description: Install the fluxstream client and server.
---

import { Steps } from 'fumadocs-ui/components/steps';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Docker

<Callout type='idea'>
  Install Docker from [here](https://docs.docker.com/get-started/get-docker/), if you have not already.
</Callout>

Docker is essential for running the fluxstream server and client in a single command with the CLI, fluxstream cli wraps docker-compose commands, hiding away complexity.

### Fluxstream CLI Installation
<TerminalCard
title="Linux / macOS"
>
```bash
curl -fsSL https://raw.githubusercontent.com/scythe504/fluxstream/main/scripts/install.sh | bash
```
</TerminalCard>
<center>OR</center>
<TerminalCard
  title="Windows"
>
```powershell
irm https://raw.githubusercontent.com/scythe504/fluxstream/main/scripts/install.ps1 | iex
```
</TerminalCard>
### Verify Install
To check if the CLI application has been successfully installed run:
<TerminalCard title="Terminal">
```sh
fluxstream -h
```
</TerminalCard>
It will print something like this in the terminal
<WindowCard
  title="fluxstream &nbsp;--help"
  subtitle="v0.4.0"
  dotSize="md"
  hideCopy
>
  <AnsiLogo />

  <pre className="overflow-x-auto scrollbar-hide text-left whitespace-pre font-mono text-[10px] sm:text-xs leading-relaxed">
    {"Usage:\n" +
      "  fluxstream [command]\n\n" +
      "Available Commands:\n" +
      "  "}
    <span className="text-cyan-400">help</span>{"        Help about any command\n" +
      "  "}
    <span className="text-cyan-400">setup</span>{"       Sets up required configs and docker engine (if not installed)\n" +
      "  "}
    <span className="text-cyan-400">start</span>{"       Start the Fluxstream server\n" +
      "  "}
    <span className="text-cyan-400">status</span>{"      Shows the status of the server whether running or not\n" +
      "  "}
    <span className="text-cyan-400">stop</span>{"        Stops the Fluxstream server\n" +
      "  "}
    <span className="text-cyan-400">where</span>{"       Prints the url for the web app\n\n" +
      "Flags:\n" +
      "  "}
    <span className="text-yellow-500">-h, --help</span>{"      help for fluxstream\n" +
      "  "}
    <span className="text-yellow-500">-v, --version</span>{"   version for fluxstream\n\n" +
      'Use "fluxstream [command] --help" for more information about a command.'}
  </pre>
</WindowCard>

## Manual Installation

Manual installation requires **Go (Golang)**, **Node.js**, and **FFmpeg** to be installed and available on your system path.

<Callout type="info" title="Prerequisites">
  Before compiling, install the runtimes from their official websites:
  - [Node.js](https://nodejs.org/)
  - [Go (Golang)](https://go.dev/)
  - [FFmpeg](https://ffmpeg.org/)
</Callout>

### 1. Backend (Server) Setup

Clone the API server repository, copy the environment file, and run the backend service:

<TerminalCard title="Compile Backend">
```bash
git clone https://github.com/scythe504/fluxstream.git
cd fluxstream
cp .env.example .env
go run cmd/api/main.go
```
</TerminalCard>

The API service runs locally on `http://localhost:8080`.

### 2. Frontend (Client) Setup

Clone the web client repository, install the packages, and run the build scripts:

<TerminalCard title="Compile Frontend">
```bash
git clone https://github.com/scythe504/fluxstream-web.git
cd fluxstream-web
cp .env.example .env
npm install
npm run build
npm run start
```
</TerminalCard>

The web interface is available at `http://localhost:3000`.

## License

Fluxstream is open-source software licensed under the **GNU Affero General Public License v3.0 (AGPLv3)**.  
See [LICENSE](./LICENSE) for details.
